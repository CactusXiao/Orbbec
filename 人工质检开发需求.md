# 人工质检 Worker 前端开发需求

## 1. 开发目标

现在需要开始编写真正用于生产流程的**人工质检 Worker 前端**。

首先，请完整、详细阅读当前项目中的：

- 系统整体架构；

- 后端 API 与 Worker 的交互契约；

- Task / Episode / Lease / 提交结果等数据结构；

- NAS 数据组织方式；

- `label_origin` 文件夹中的投影与可视化实现。


在充分理解现有系统后，开发一套运行在 **Ubuntu** 上的人工质检前端。

不要重新设计与现有后端不兼容的协议。Task 获取、Episode 租借、续租、放弃、完成提交等操作，都必须严格遵循当前后端交互契约。

本需求中的人工质检就是后端 Workflow 中的 **QC 阶段**，不是独立的 Demo，也不是 manual label/correction 阶段。

正式生产流程为：

```
collection
  ↓
auto label
  ↓
episode-level 3D / MANO
  ↓
人工 QC
  ↓
manual label / correction
  ↓
segment-level 3D / MANO
```

人工 QC 只负责判断 episode-level MANO 结果是否可接受，并提交 QC 结果。它不编辑 2D 点，也不生成 manual correction 结果。

人工 QC 的结果分为三类：

1. **全通过**；

2. **存在坏帧区间**；

3. **整 Episode 异常**。


其中“存在坏帧区间”会进入后续 manual label/correction 流程；“整 Episode 异常”应进入异常 Episode 待处理状态，不进入人工纠偏。

---

# 2. 整体工作流

人工质检 Worker 的主流程为：

```
启动 Worker
    ↓
获取所有待质检任务
    ↓
Task 选择界面
    ↓
选择某个 Task
    ↓
Episode 选择界面
    ↓
选择某个 Episode
    ↓
向后端正式租借 Episode
    ↓
从 NAS 获取质检所需数据
    ↓
解码六路 H.265 RGB 视频
    ↓
进入人工质检界面
    ↓
逐采样帧检查
    ↓
记录坏帧区间
    ↓
Episode 检查完成
    ↓
汇总质检结果
    ↓
提交后端并报告任务完成
    ↓
返回 Episode 选择界面
```

人工 QC 位于后端 `qc` 阶段。后端需要为人工 QC 支持：

- 查询当前可租借的 QC Episode 列表；

- 按机器区分 QC Worker；

- 由 QC Worker 在租借时指定本次租约占用时间；

- 租期内持续占用该 Episode；

- 租约过期后自动把 Episode 放回待 QC 列表；

- QC 通过、QC 坏帧、整 Episode 异常三种提交结果。

---

# 3. Task 选择界面

Worker 启动后，应首先向后端查询当前所有**待人工质检的 QC Episode**，并按 Task 分类显示。

这里的“待质检 Episode”只指后端当前返回的 **可租借 QC Episode 列表**。

后端返回的列表中只应包含当前可以租借的 Episode；不可租借、被其他 Worker 租借、已完成、已异常等 Episode 不需要展示在普通 QC 领取列表中。

按照 `task` 对 Episode 进行分类显示。

Task 界面至少需要展示：

- Task 名称；

- 待质检 Episode 数量；

- 其他当前契约中已有且对人工选择有意义且不会造成雍肿的信息。

点击某个 Task 后，进入该 Task 对应的 **Episode 选择界面**。

注意：当前机器本地保留的有效 QC 进度，即使不在后端可租借列表中，也必须额外合并显示。

---

## 3.1 本地保留任务显示

如果某个 Task 下存在本地保存了质检进度且仍处于有效租期内的 Episode，需要在 Task 界面中明显标记。

例如可以在最左侧添加红色圆点并在合适位置显示：

```
存在进行中的质检任务
最快过期：00:37:12
```

Task 层显示的倒计时，应取：

> 当前 Task 下所有本地保留 Episode 中，**最早过期的一个 Episode 的剩余时间**。

倒计时需要实时刷新。

---

# 4. Episode 选择界面

进入某个 Task 后，显示该 Task 下所有可供人工质检的 Episode。

普通后端列表中拿到的 Episode 都视为可租借，不需要展示不可租借 Episode。

除了后端返回的可租借 Episode 外，还需要显示当前机器本地保留且仍在有效租期内的 Episode。

如果某个 Episode 存在当前 Worker 的本地进度记录，需要特别标记，例如：

```
进行中
租期剩余：01:24:35
```

倒计时需要实时更新。

---

# 5. Episode 租借

用户在 Episode 选择界面点击一个可质检 Episode 后：

1. 向后端发送正式租借请求，并传入本次希望占有的租约时间；

2. 必须等待后端确认租借成功；

3. 只有租借成功后，才能进入数据准备和质检流程；

4. 如果租借失败，例如已被其他 Worker 抢占，需要刷新 Episode 状态。


对于已经存在本地保存进度的 Episode，应按照当前后端契约恢复对应租约或确认租约仍然有效，而不能创建冲突的新租约。

QC Worker 按机器进行区分。生产环境中机器、程序和端口会固定，只要能够稳定区分不同机器即可。

首次租借的默认租约时间为 **10 分钟**，并且必须可以通过 `.env` 配置。这个默认值只表示默认占用时间，不表示最长保留时间；人工 QC 不设置最长保留时间。

对于本机本地保留的 Episode：

- 如果后端租约仍有效，允许直接恢复；

- 如果后端租约已过期，不能直接恢复，需要先尝试重新租借；

- 如果重新租借成功，则继续使用本地进度；

- 如果重新租借失败，则走放弃任务流程，撤销本地未提交修改，释放相关租约状态，并直接删除本地进度。

---

# 6. Episode 数据获取

成功租借 Episode 后，根据 Episode 返回的数据路径，从 NAS 获取：

1. episode-level **3D / MANO 手部结果**；

2. 六个 Orbbec 相机对应的 RGB H.265 视频；

3. 对应六个相机正确的：

    - 相机内参；

    - 相机外参；

    - 其他完成 3D → 2D 投影所需标定信息。


所有文件位置和格式优先从现有系统契约、数据目录结构中确认，不要自行假设。

人工 QC 只检查 episode-level MANO 结果。它不读取 manual correction 后生成的 segment-level MANO patch 作为 QC 输入。

---

# 7. H.265 视频解码

六路 Orbbec RGB 数据为 H.265 文件。

进入正式质检前，需要将六个 H.265 文件在本地解码。

解码结果保存至本地临时目录，例如：

```
tmp/
```

具体临时目录结构应合理组织，例如按照：

```
tmp/
  episode_id/
    camera_0/
    camera_1/
    camera_2/
    camera_3/
    camera_4/
    camera_5/
```

组织。

---

## 7.1 解码进度界面

视频解码过程中，前端不能无响应。

需要显示明确的加载页面，例如：

```
正在准备质检数据
正在解码 RGB 视频……

Camera 1  ███████████████░░░  82%
Camera 2  ██████████████████  100%
Camera 3  ███████████░░░░░░░  61%
...
```

至少需要展示：

- 当前正在进行视频解码；

- 六路视频各自状态；

- 总体进度；

- 如果可以获得帧数，则显示：

    - 已解码帧数；

    - 总帧数；

- 解码失败时明确提示对应 Camera 和错误原因。


解码过程不得阻塞 UI 主线程。

---

# 8. 采样策略

人工质检不检查所有帧，而是按照固定间隔进行采样。

默认：

```
sample_interval = 10
```

即：

```
0
10
20
30
40
...
```

每 10 帧检查一次。

该参数必须可以通过 `.env` 文件配置，例如：

```
QC_SAMPLE_INTERVAL=10
```

如果配置不存在，则默认值为：

```
10
```

后续所有涉及“N 帧”的操作，N 均指当前配置的 `sample_interval`，不要在代码中硬编码 10。

---

# 9. 3D 手部结果投影

对于每一个需要质检的帧：

1. 获取当前帧对应的 episode-level MANO 3D 手部关节结果；

2. 获取六个相机对应的 RGB 图像；

3. 使用正确的相机内参和外参；

4. 将 3D 手部关节分别投影到六个 RGB Camera View 上；

5. 将投影后的 2D 手部关节绘制到 RGB 图像上。


---

## 9.1 投影逻辑参考

`label_origin` 文件夹中的实现是正确参考。

这里需要对齐的是 `label_origin` / 当前 `label` 工具中的 **MANO 投影显示路径**：

```
episode-level MANO 3D
  +
camera_params.json
  +
extrinsics.json
  ↓
投影到每个 RGB Camera View
  ↓
绘制 2D Skeleton
```

不要把 `label` 工具的所有输入来源都视作人工 QC 输入。`label` 工具有 Pred / Correct / Last / Tracking / Scratch 等 2D 纠偏来源；人工 QC 不使用这些编辑来源。人工 QC 只需要读取 episode-level MANO 3D，并按照 label 中 MANO 投影和绘制规则显示。

必须重点复用或对齐其中：

- 相机内外参读取方式；

- 坐标系转换方式；

- 3D → Camera 坐标变换；

- Camera → Pixel 投影；

- 畸变处理，如果存在；

- 左右手判断；

- 关节点拓扑连接方式；

- 投影边界处理。


不要重新凭经验实现一套新的投影公式。

---

## 9.2 手部可视化效果

RGB 图像上手部关节点的视觉表现，也需要和 `label_origin` 对齐，包括但不限于：

- 关节点位置；

- 点大小；

- 骨架连线；

- 左右手颜色；

- 可见性处理；

- 越界点处理；

- 其他现有视觉规则。


目标是让人工质检界面看到的投影效果与现有正确标注工具保持一致。

---

# 10. 主质检界面

正式进入 Episode 质检后，屏幕主体区域同时显示：

```
Camera 1    Camera 2    Camera 3
Camera 4    Camera 5    Camera 6
```

即六张图按照合理的 2×3 或 3×2 布局同时展示，需要让有效显示区域尽量大。

每个 View 显示：

```
RGB Frame
+
当前帧 3D 手部关节投影后的 2D Skeleton
```

六个 Camera 必须对应同一个时间帧。

---

# 11. 主质检控制 Bar

界面底部需要设置一个固定控制 Bar。

正常检查模式至少包含：

```
[上一采样帧]

当前帧：120 / 3840
采样间隔：10

[通过]
[不通过]
[Episode 异常]

[下一采样帧]

[提交]
[返回 Episode 列表]
[退出程序]
```

---

# 12. 正常采样帧导航

正常状态下：

### 上一采样帧

跳转：

```
current_frame - sample_interval
```

例如：

```
120 → 110
```

---

### 下一采样帧

跳转：

```
current_frame + sample_interval
```

例如：

```
120 → 130
```

不能超过 Episode 最后一帧。

---

# 13. “通过”操作

当前采样帧检查正常时，点击：

```
通过
```

表示：

> 当前采样检查点没有发现需要人工标记的异常。

之后进入下一个采样帧。

---

# 14. “不通过”操作

如果当前采样帧发现问题，点击：

```
不通过
```

此时不能立即只把当前一帧标记为坏帧。

系统需要切换到：

# 坏帧区间选择模式

因为实际错误可能发生在：

```
上一采样帧
        ↓
若干正常帧
        ↓
真正错误起始帧
        ↓
当前采样帧
        ↓
若干错误帧
        ↓
真正错误结束帧
        ↓
下一采样帧或下几个采样帧
```

所以需要进一步人工确定坏帧区间。

---

# 15. 坏帧区间选择模式

假设：

```
sample_interval = N
```

当前“不通过”的采样帧为：

```
F
```

进入坏帧区间选择模式后，底部 Bar 切换为更加精细的逐帧控制。

至少包含：

```

[向前一帧]
[向前 N 帧]
[设为坏帧起点]

当前帧：xxx

[向后一帧]
[向后 N 帧]
[设为坏帧终点]

当前选择：
Start = xxx
End   = xxx

[确认坏帧区间]
[撤销]
```

---

# 16. 坏帧区间导航约束

## 16.1 向后一帧

即：

```
frame + 1
```

---

## 16.2 向后 N 帧

即：

```
frame + sample_interval
```

用于快速查看后续采样位置附近情况。

需要做好 Episode 结束边界处理。

---

## 16.3 向前一帧

即：

```
frame - 1
```

但向前最多只能回退到：

```
当前异常采样帧 F - sample_interval
```

即：

> 不能检查或越过上一个采样帧。

例如：

```
sample_interval = 10
当前异常采样帧 F = 120
前一采样帧 F' = 110
```

允许检查：

```
111 ~ ...
```

但不能跳到：

```
110
109
108
...
```

因为前一个采样区间已经由上一轮人工判断覆盖。

---

# 17. 坏帧起点与终点

在坏帧区间选择模式下，需要有两个独立按钮：

```
设为坏帧起点
```

和：

```
设为坏帧终点
```

用户可以逐帧检查六个 Camera View，从而确定：

```
bad_frame_start
bad_frame_end
```

界面应实时显示：

```
坏帧起点：123
坏帧终点：137
```

并对非法范围进行限制，例如：

```
start > end
```

时不能提交。

采用全闭区间

---

# 18. 确认坏帧区间

当用户确认：

```
bad_frame_start
bad_frame_end
```

后，点击：

```
确认坏帧区间
```

系统需要：

1. 将该区间写入当前 Episode 的本地质检状态；

2. 记录：

    ```
    [bad_frame_start, bad_frame_end]
    ```

3. 返回普通采样检查界面；

4. 自动定位到：

    > **坏帧结束后的第一个采样帧**


---

# 19. 坏帧后的自动跳转

例如：

```
sample_interval = 10

当前采样帧 = 120

用户最终确定坏帧：
123 ~ 147
```

那么完成标记后，不应该重新从：

```
130
140
```

继续检查。

而应该直接跳到：

> 第一个严格位于 147 之后的采样帧。

例如采样序列为：

```
0, 10, 20, ..., 140, 150, 160
```

则跳转：

```
150
```

这样可以避免对已经确定属于坏帧区间的采样点进行重复检查。

---

# 20. 撤销坏帧选择

坏帧区间选择模式中必须存在：

```
撤销
```

点击后：

1. 不记录任何坏帧；

2. 删除此次临时选择的：

    - start；

    - end；

3. 回到普通质检界面；

4. 回到原来的采样帧；

5. 该采样帧重新处于：

    ```
    未判断
    ```

    状态；

6. 用户需要重新选择：

    ```
    通过 / 不通过
    ```


---

# 21. Episode 完整质检流程

整个 Episode 的流程类似：

```
Frame 0
  ↓
通过
  ↓
Frame 10
  ↓
通过
  ↓
Frame 20
  ↓
不通过
  ↓
精细检查
  ↓
坏帧 17~28
  ↓
记录
  ↓
跳转 Frame 30
  ↓
通过
  ↓
...
  ↓
Episode End
```

重复直到 Episode 所有需要检查的范围全部完成。

---

# 22. 质检结果的数据结构

在本地维护一个清晰的 Episode QC 状态，例如概念上：

```
{
  "task_id": "...",
  "episode_id": "...",
  "worker_machine_id": "...",
  "sample_interval": 10,
  "current_frame": 150,
  "result_type": "bad_frames",
  "bad_frame_ranges": [
    [17, 28],
    [137, 152]
  ]
}
```

实际字段名称应按照当前后端契约设计。

`result_type` 概念上至少包含：

- `passed`：全 Episode 通过，没有坏帧段；

- `bad_frames`：存在坏帧区间；

- `bad_episode`：整 Episode 异常，不进入人工纠偏。


提交前需要：

- 排序坏帧段；

- 检查是否合法；

- 如果出现重叠或连续区间，合并为一个区间；如果出现两个区间之间空隙小于M帧（env可配置，默认M=5），也进行合并

- 保证最终提交格式完全符合后端要求。


---

# 23. Episode 完成与提交

控制 Bar 中需要提供：

```
提交
```

按钮。

正常情况下，应在 Episode 检查完成后进行提交。

点击提交后：

1. 检查当前 Episode QC 是否完整；

2. 根据后端交互契约生成最终人工质检结果；

3. 汇总所有坏帧区间；

4. 向后端提交质检结果；

5. 向后端报告该 Episode 人工质检任务完成；

6. 等待后端成功响应；

7. 成功后清理对应本地进度；

8. 清理可以安全删除的临时解码文件；

9. 返回 Episode 选择界面。


禁止出现：

```
本地显示提交成功
但后端实际没有完成任务
```

的状态。

提交必须等待后端返回确认信息后，前端才能进入“提交成功 / 已完成”状态。

## 23.1 提交结果类型

人工 QC 最终提交结果分为三类：

### 23.1.1 全通过

如果没有任何坏帧区间，也没有整 Episode 异常，则提交：

```
passed = true
segments = []
```

后端应将该 Episode 视为 QC 通过，并继续进入后续 finalized / 完成流程。

### 23.1.2 存在坏帧区间

如果存在一个或多个坏帧区间，则提交：

```
passed = false
segments = [
  {
    "start_frame": 17,
    "end_frame": 28
  }
]
```

坏帧段不需要填写 reason。后端只需要知道这些是真实坏帧区间。

后端收到该结果后，应根据这些区间进入 manual label / correction 流程。

### 23.1.3 整 Episode 异常

控制 Bar 中需要提供：

```
[Episode 异常]
```

如果用户判断当前 Episode 整体异常，例如数据整体不可用、结果整体不可信、或不适合进入普通坏帧段纠偏流程，则点击该按钮。

此时必须弹窗二次确认。

确认后，前端向后端提交：

```
result_type = bad_episode
```

或后端契约中等价的“bad episode / abnormal episode”字段。

整 Episode 异常不应被转换为覆盖全 Episode 的普通坏帧区间。

后端应将该 Episode 标记为异常待处理状态，阻塞其进入 manual label / correction 流程。

---

# 24. 返回 Episode 选择界面

控制 Bar 中需要：

```
返回 Episode 列表
```

如果当前 Episode 尚未完成，需要认为这是一次主动中断。

必须弹窗询问如何处理当前进度。

---

# 25. 退出程序

控制 Bar 中需要：

```
退出程序
```

如果当前没有正在进行的 Episode，可以直接退出。

如果当前存在未完成 Episode，则必须进入退出处理流程。

---

# 26. 主动中断未完成 Episode

对于：

- 返回 Episode 界面；

- 主动退出程序；

如果 Episode 尚未完成，需要弹窗：

```
当前 Episode 尚未完成。

是否保留当前质检进度？

[放弃任务]
[保留进度]
[回到任务]
```

---

# 27. 放弃任务

如果用户选择：

```
放弃任务
```

系统需要：

1. 向后端调用“放弃租借 / 释放任务”的接口；

2. 根据当前后端契约撤销当前未提交的任务状态；

3. 删除对应本地进度；

4. 撤销本 Episode 因人工质检产生的尚未正式提交的相关文件修改；

5. 清理临时文件；

6. 返回 Episode 界面或退出程序。


需要保证：

> 放弃 Episode 后，不留下一个看起来仍被当前 Worker 占用的租约。

如果是因为本地保留进度已过期且重新租借失败而进入放弃流程，本地进度直接删除，不保留用于恢复的副本。

---

# 28. 保留进度

如果用户选择：

```
保留进度
```

需要弹出租期延长设置。默认保留时间为 **10 分钟**，并且必须可以通过 `.env` 配置。

例如：

```
保留当前任务

小时：[ 0 ]
分钟：[ 10 ]

[确认]
[取消]
```

用户输入：

```
xx 小时
xx 分钟
```

后：

1. 计算需要保留的租期；

2. 根据当前后端契约向后端发起续租 / 延长 lease 请求；

3. 后端确认成功后；

4. 本地持久化当前质检进度；

5. 保存新的租期截止时间；

6. 然后：

    - 返回 Episode 界面；

    - 或退出程序。


保留进度时，后端租约在用户指定的租期内持续占用该 Episode。该租期不设置最长上限。

---

# 29. 本地进度保存

保留任务时，本地至少需要持久化：

```
task_id
episode_id
lease_id（如果契约存在）
lease_expire_time
worker_machine_id
sample_interval
current_frame
已经完成检查的位置
result_type
bad_frame_ranges
版本信息
```

必须保证程序重启后能够恢复。

不能仅将状态保存在内存中。

本地状态只保存恢复人工 QC 工作所必需的信息，例如当前采样帧、已确认坏帧段、检查完成位置、租约截止时间等。

RGB 解码缓存、临时图片等可以删除，不要求作为保留进度的一部分持久化。

---

# 30. 意外退出处理

需要处理程序异常退出，例如：

- 程序 crash；

- UI 进程异常结束；

- SIGTERM；

- SIGINT；

- 系统关机；

- 其他可捕获异常。


对于这种情况：

> 默认保留当前进度，并尝试将租期延长 **10 分钟**。

即默认：

```
QC_CRASH_LEASE_EXTENSION_MINUTES=10
```

默认值：

```
10
```

应尽最大可能：

1. 保存当前本地 QC 状态；

2. 向后端请求额外延长 10 分钟租期；

3. 再退出进程。


---

## 30.1 关于真正不可捕获的退出

例如：

```
SIGKILL
断电
系统直接崩溃
```

程序无法在退出瞬间执行网络请求。

因此本地进度必须采用：

> **持续增量持久化**

而不是只在退出时保存。

至少在以下事件发生时更新本地状态：

- 每次“通过”；

- 每次确认坏帧区间；

- 当前采样帧改变；

- lease 更新；

- Episode 状态改变。


这样即使进程突然死亡，也能在下次启动时尽可能恢复。

---

# 31. 本地进度恢复

程序重新启动时，需要扫描本地保存的进度。

然后与后端 Lease 状态进行校验。

可能情况：

### Case 1：Lease 仍有效

显示：

```
继续质检
剩余：00:08:21
```

允许恢复。

---

### Case 2：Lease 已过期

本地状态不能直接作为有效任务继续。

需要根据现有后端协议：

- 尝试重新租借；

- 如果重新租借成功，则继续使用本地 QC 进度；

- 如果无法租借，则走放弃任务流程，撤销本地未提交修改，并直接删除本地 QC 状态。

不能假设过期 Lease 仍然有效。

---

### Case 3：后端任务已经完成或者被他人租借

删除过期本地 QC 状态，不允许重复提交。

---

# 32. 过期倒计时

Task 和 Episode 页面都需要显示保留任务的 lease 剩余时间。

例如：

```
00:34:27
```

Episode 页面：

```
Episode 001
进行中
剩余：00:34:27
```

Task 页面：

```
Task A
存在进行中的 Episode
最快过期：00:12:03
```

倒计时需要实时刷新。

临近过期时建议有明显提示。

---

# 33. 推荐状态机

建议将人工质检流程明确建模为状态机，而不是使用大量散乱的 UI Boolean。

例如：

```
TASK_SELECTION
        ↓
EPISODE_SELECTION
        ↓
LEASE_ACQUIRING
        ↓
DATA_PREPARING
        ↓
VIDEO_DECODING
        ↓
QC_SAMPLING
        ↓
BAD_RANGE_SELECTING
        ↓
QC_SAMPLING
        ↓
QC_COMPLETED
        ↓
SUBMITTING
        ↓
EPISODE_SELECTION
```

中断流程：

```
QC_SAMPLING
      ↓
EXIT_DIALOG
   ↙       ↘
ABANDON   PRESERVE
   ↓         ↓
RELEASE    EXTEND_LEASE
   ↓         ↓
EXIT      SAVE_STATE
```

---

# 34. 配置项

至少将以下参数放入 `.env`：

```
# 每隔多少帧人工检查一次
QC_SAMPLE_INTERVAL=10

# 首次租借和手动保留进度时默认占用多少分钟
QC_DEFAULT_LEASE_MINUTES=10

# 程序异常退出时默认额外保留多少分钟
QC_CRASH_LEASE_EXTENSION_MINUTES=10

# 本地临时解码目录
QC_TMP_DIR=./tmp

# 本地进度保存目录
QC_STATE_DIR=./qc_state

# 当前机器的 QC Worker 标识。生产环境中机器、程序和端口固定，只要能稳定区分机器即可
QC_WORKER_MACHINE_ID=...

# 后端地址，nas相关配置等
...

```

不要和其他worker和前后端的env混用


---

# 35. 性能要求

由于六路图像需要同时展示，必须注意：

### 不要

每切换一帧就：

```
重新启动 ffmpeg
重新解码整段 H.265
重新加载完整视频
```

应该在进入 Episode 时完成必要的解码或建立合理缓存。

UI 切换采样帧时，应尽量做到接近即时响应。

充分利用硬件，充分且合理的使用并行多线程，减少阻塞。

---

# 36. 并发与 UI 响应

以下操作不能阻塞 UI 主线程：

- 后端 API 请求；

- NAS 文件读取；

- H.265 解码；

- 六视角图像读取；

- 3D → 2D 投影；

- 提交任务；

- lease 延长。


尤其是视频解码阶段，必须能够实时更新进度条。

---

# 37. 阻塞性错误处理

开发阶段，所有阻塞性错误都先按“不能继续当前 Episode”处理。

例如：

- 后端请求失败；

- NAS 路径不可读；

- H.265 解码失败；

- episode-level MANO 结果缺失或格式错误；

- calibration 缺失或无法完成投影；

- 提交结果失败。


处理原则：

1. 前端应明确提示用户当前 Episode 无法继续；

2. 如果当前 Episode 已成功租借，优先向后端释放租约；

3. 释放成功或失败都必须在终端输出详细原因；

4. 不要把阻塞性错误伪装成 QC 通过或普通坏帧；

5. 不要在后端未确认时显示提交成功。


---

# 38. 异常 Episode 专页

后端网页需要增加异常 Episode 专页，用于展示所有被人工 QC 标记为整 Episode 异常的 Episode。

异常 Episode 不进入 manual label / correction 流程。

异常 Episode 专页至少需要支持：

- 展示异常 Episode 列表；

- 点击某个异常 Episode 查看详情；

- 对单个异常 Episode 执行真实后端操作。


单个异常 Episode 页面至少需要提供：

```
[重新推回 QC]
[重新跑 3D]
[标记废弃]
```

这些按钮必须真实调用后端对应操作，不能只是前端状态变化。

语义要求：

- **重新推回 QC**：将该 Episode 重新放入待人工 QC 列表；

- **重新跑 3D**：将该 Episode 重新进入 episode-level 3D / MANO 阶段；

- **标记废弃**：将该 Episode 标记为废弃，不再进入后续生产流程。


---

# 39. 文件修改原则

人工质检过程中的中间结果，应尽可能写入本地 QC state。

不要过早修改正式结果文件。

推荐：

```
episode-level MANO 结果：只读
↓
人工 QC state：单独保存
↓
最终 Submit
↓
按照后端契约生成正式 QC 结果
```

这样才能可靠实现：

```
放弃任务 → 完整撤销
```

---

# 40. 开发顺序

不要直接开始凭空搭 UI。

首先完成：

## Step 1：阅读项目

重点确认：

```
backend API
worker contract
task schema
episode schema
lease schema
submit schema
NAS layout
label_origin
calibration
episode-level MANO 3D format
QC list / lease contract
abnormal episode state contract
```

---

## Step 2：输出架构理解

先明确：

```
Task 如何获取
Episode 如何获取
Lease 如何创建
Lease 如何续期
Lease 如何释放
Episode 如何完成
QC Result 如何提交
NAS 路径如何解析
episode-level MANO 3D 数据格式
Calibration 格式
label_origin / label 的 MANO 投影入口
异常 Episode 如何展示和重新流转
```

---

## Step 3：设计 Worker 状态机

完成：

```
Task Selection
Episode Selection
Preparing
QC
Bad Range Selection
Submit
Preserve
Resume
Abandon
Bad Episode
```

---

## Step 4：实现数据层

优先实现：

```
Backend Client
Lease Manager
Local State Manager
NAS Loader
Video Decoder
Projection Renderer
QC Result Builder
```

让 UI 不直接操作底层文件和网络协议。

---

## Step 5：实现 UI

再实现：

```
Task 页面
Episode 页面
Decode 页面
QC 页面
Bad Range 页面
Exit Dialog
Resume 状态
异常 Episode 专页
```

---

## Step 6：联调完整闭环

必须至少完整跑通：

```
领取 Episode
→
解码
→
检查
→
标记坏帧
→
提交
→
后端完成
```

以及：

```
领取
→
检查一半
→
保留
→
退出
→
重新启动
→
恢复
→
继续
→
提交
```

以及：

```
领取
→
检查
→
放弃
→
后端 Lease 释放
→
本地状态清理
```

以及：

```
领取
→
判断整 Episode 异常
→
提交 bad_episode
→
后端进入异常待处理
→
异常 Episode 专页可见
→
重新推回 QC / 重新跑 3D / 标记废弃
```

---

# 41. 最终验收标准

完成后的 Worker 至少需要满足：

- 可以正常显示后端所有待人工质检 Task；

- 可以正确进入 Task 下的 Episode 列表；

- 普通 QC 列表只展示后端返回的可租借 Episode；

- 本机有效保留任务即使不在后端可租列表中，也能显示和恢复；

- 可以正确租借 Episode；

- 首次租借默认占用 10 分钟，且可通过 `.env` 配置；

- 可以从 NAS 获取正确 Episode 数据；

- 可以解码六路 Orbbec H.265 RGB；

- 解码过程有可见进度；

- 可以按照 `.env` 设置的采样间隔检查；

- 默认每 10 帧检查一次；

- 可以使用正确 Calibration 将 3D 手部关节投影到六路 RGB；

- 投影视觉效果与 `label_origin` 对齐；

- 人工 QC 对齐的是 label 的 MANO 投影显示路径，而不是 label 的 2D 纠偏输入来源；

- 六视角能够同时显示；

- 可以逐个采样帧选择通过 / 不通过；

- 不通过后可以精细确定坏帧起止；

- 向前逐帧检查不能越过上一个采样帧；

- 可以确认坏帧范围；

- 可以撤销坏帧选择；

- 确认坏帧后自动跳过该坏帧区间；

- 可以完整检查到 Episode 结束；

- 可以正确汇总所有坏帧；

- 可以按照后端契约提交 QC Result；

- 可以提交全通过、坏帧区间、整 Episode 异常三种 QC 结果；

- 整 Episode 异常不会被当作覆盖全 Episode 的普通坏帧区间；

- 异常 Episode 能在后端专页展示，并可重新推回 QC、重新跑 3D、标记废弃；

- 可以正确完成任务；

- 可以返回 Episode 列表；

- 未完成退出时可以选择放弃；

- 未完成退出时可以选择自定义时间保留；

- 保留任务时可以正确延长后端 lease；

- 本地可以持久化 QC 状态；

- 程序重启后可以恢复有效任务；

- 意外退出默认按 10 分钟策略保护进度；

- Task 页面能够显示进行中任务及最快过期倒计时；

- Episode 页面能够显示具体 lease 倒计时；

- 后端、NAS、Decode、Projection 等错误都有明确反馈；

- 开发阶段阻塞性错误会释放租约，并在终端输出详细原因；

- 整个 UI 在下载、解码和后端通信期间不能假死。


---

# 42. 一个重要实现原则

这次开发的是**真正进入工作流程使用的人工质检 Worker**，不是 UI Demo。

因此优先级依次为：

```
后端契约正确
>
任务状态和 Lease 正确
>
数据与帧对应正确
>
3D → 2D 投影正确
>
QC 状态与结果正确
>
异常恢复正确
>
UI 易用性
>
视觉美化
```

尤其不要为了快速实现 UI 而：

- mock 后端流程；

- 自行假设 Task / Episode 数据结构；

- 绕过 lease；

- 自行猜测 Calibration；

- 复制一套与 `label_origin` 不一致的投影；

- 把 label 的 2D 编辑来源误当成人工 QC 输入；

- 只在内存中保存质检进度；

- 在提交前直接破坏原始自动标注数据。


所有这些核心行为都必须以当前项目已有架构和后端契约为准。
