# 2026-09-10：去除视图补边（v4.4）

派生视频仍使用原封装，显示矩形只包含真实图像像素，原图/MANO 裁取相同范围。
原缓存通过读取已有原图的尺寸补充显示信息，无需重新渲染或编码。
QC 每行按各机位实际比例分配宽度、同一行高度一致，视图之间仅留 2 px。
不同画面比例会使上下两行的列边界不同；全部视图完整保留，不通过裁切或拉伸填满窗口。
窗口比例与组合画面不同时，仅在整个视图区外保留适配余量。

真实六视图已确认去边、透明度和播放暂停正常。检查覆盖图像四角完整、两层坐标偏移、
多窗口尺寸下的排布、旧视频缓存兼容，以及原 QC 操作和提交回归。
截图：output/outsourcing-validation/qc-content-bounds-v44.png。

# 2026-09-10：扩大图像区域（v4.2）

按用户要求精简外包工作区：隐藏各视图编号标题（保留悬停提示和可访问名称），
压缩页头/工具栏/帧信息/进度条，去除多层留白；QC 缓冲详情移入悬停提示，缓冲中仍可见。
Label 进度侧栏默认收起，可通过“标注进度”展开；关键帧状态、错误、操作按钮和原业务步骤保留。
任务选择/登录页独立，原桌面 Label/QC 不修改。

1280×720 实际浏览器检查通过：QC 六视图显示、Label 单视图、进度侧栏展开/收起。
沿用 4 项暂停回归检查并通过。截图：output/outsourcing-validation/label-compact-v42.png、qc-compact-v42.png。

# 2026-09-10：QC 暂停修复（v4.1）

开始播放的意图现在在异步定位之前登记，准备/缓冲期间暂停始终可用。
暂停使旧播放请求失效；旧原生 play() 的迟到完成或 AbortError 不得重新启动视频或取消新请求。
暂停按钮文字只在状态变化时更新，避免每帧替换正在点击的文字节点。

`node tests/browser_player_pause.mjs` 包含 4 个回归场景：准备中取消、缓冲后保持暂停、旧播放拒绝与新请求竞争、
迟到播放完成。旧正式版在第一项失败，修复后全部通过。
另有 11 项 QC 原版状态对照、播放完成及视频图层测试通过。
隔离浏览器真实视频验证：播放后立即暂停停在第 8 帧，等待后不自动继续；可恢复播放、再次暂停和逐帧。

# 2026-09-10：缓存与 MANO 透明度（v4）

本次仅更新 remote_frontend 外包分支。实验室隔离根：
`/home/ubuntu/demo/outsourcing-validation-20260909/layered-v4`。
登录沿用 verify-admin 测试账号；任务租约、草稿和 NAS 输出来自独立测试库/副本。

- 真实六视角 180 帧视频（6 秒）经 VPN/SSH 到本机浏览器，MSE 全段预取。
  播放结束状态正确；一次完整播放后浏览器计数为 0 dropped / 184 total（包含定位解码），不能换算为严格帧率 SLA。
- 第 89 → 90 帧跨片段定位、坏帧 90–100 起止和确认、MANO 0/25/50/100% 本地调整通过。
- 隔离后端临时媒体请求审计：已缓存范围内播放/暂停/逐帧/透明度调整为 0 新增媒体请求。
  点击高清检查后产生 6 张 raw_frames + 6 张 frames 请求，返回视频无静态图自动请求。
- Label 在第 90 帧时已预取当前机位 91–100 和其他机位 90–92。
  切到 00/91、02/91 均无重复图片请求；只补充新窗口内预取。
- 编码：同一 90 帧素材，原串行约 7.77 秒；4 路有界 JPEG 解码/拼接约 2.08 秒；
  JPEG 按预览比例解码后约 1.12 秒。最终两段为 1.147/1.105 秒、每段约 2.30 MB。
  这不包括原 H.265 解码、MANO 渲染和网络传输时间，不是整体冷启动耗时。
- 自动检查涵盖原版 QC 状态对照、账号/权限/提交、生命周期、帧映射、图层尺寸、H.264 时间戳及缓冲窗口规划。
  本机共运行 76 项，74 通过、2 项按环境条件跳过。
- 长视频缓冲窗口以 300 秒/100 个片段的规划用例覆盖；实际素材播放仍为 6 秒。
  多人并发、持续限速/丢包、低配 Safari 和真实长片段吞吐尚待专项验收。

截图保存在工作区 output/outsourcing-validation：qc-opacity-0-v4.png、qc-hq-opacity-v4.png、label-prefetch-v4.png。

# 当前状态说明

以下保留历史实验记录，旧版共享链接、按操作员单实例及曾修改的网页流程均已被 v3 替代。
当前流程与登录规范见 [BROWSER.md](BROWSER.md)、[AUTHENTICATION.md](AUTHENTICATION.md)。
历史 PID 仅代表当时状态；本次测试期间实验室主机重启，原有进程已退出。

# 实验室主机 → 本机浏览器验证（2026-09-09）

结论：浏览器方向可行，但整页远程桌面不应作为外包生产方案的默认媒体链路。
保留兼容入口用于原 UI/业务复用；正式方向为后端计算、独立视频、本地交互。

## 范围与环境

- 后端：通过用户指定 SSH 主机访问的 Ubuntu 22.04，60 GiB RAM，RTX 4090。
- 前端：本机 macOS 的 Codex 内置浏览器。原始视频和模型始终留在实验室主机。
- 数据：`/mnt/nas/xjz/task15/episode1` 的前 180 帧（6 秒、30 FPS），
  00/02/03/05/06 + Pico Ego，共六视图，复用现有 QC 的真实 MANO 渲染。
- 测试根：`/home/ubuntu/demo/outsourcing-validation-20260909`，源码快照与工作流库独立。
- 普通网络两次 ICMP RTT 为 12.039 / 12.310 ms，仅用于说明测试网络条件。
- noVNC 1.6.0 / x11vnc 0.9.16 / websockify 0.10；虚拟屏幕 1600×1000。
- 派生视频 1536×864，libx264 veryfast、4 线程、30 帧 GOP、目标 3 Mbps。

## 已测结果

| 测试 | 结果 | 必须保留的解释 |
|---|---:|---|
| 180 帧六视图预览编码 | 3.805 s，47.31 帧/s | 不包括此前原始解码/MANO 渲染时间；不是整体冷启动速度 |
| H.264 预览体积 | 2,173,766 bytes | 6 秒；平均 2.898 Mbps，不含网络协议开销 |
| 原版 QC 远程画面 | 1 秒窗口峰值 21.98 Mbps；峰值 6 次 framebuffer 更新/s | UI/分辨率与独立视频不同；framebuffer 更新不能当作源视频 FPS |
| 原版 QC 本次播放流量 | 12.87 MiB；32 次 framebuffer 更新 | 测试从第 23 帧播放到第 179 帧；计量窗口还含前后停顿，不能用其整窗平均值代表播放码率 |
| 普通网络原生视频 | 28.99 FPS；180 decoded / 6 dropped；首次出画 235.3 ms | 一次短样本，不能推导稳定 SLA |
| 4 Mbps / 每请求额外 100 ms，第一次 | 29.84 FPS；180 decoded / 0 dropped；首次出画 401 ms | 应用层每响应限速，不是完整 WAN 模拟 |
| 同条件，第二次 | 30.00 FPS；180 decoded / 0 dropped；播放中再缓冲 0 次；首次出画 464.3 ms | 6 秒短片，不代表长片/并发验收通过 |
| 已缓冲跳转第 90 帧 | 18.8 ms；显示帧 90 | 目标已在浏览器缓存，不包含网络获取时间 |
| 未缓冲跳转第 90 帧 | 965.3 ms；显示帧 90 | 4 Mbps / 额外 100 ms 条件；说明不能承诺所有跳帧都即时 |

浏览器的视频计数使用 `requestVideoFrameCallback` 和 `getVideoPlaybackQuality`。
跳帧会额外解码若干帧，所以截图在跳转后可能显示 decoded=184；播放结束时为 180。
第一次实验的 `waiting_events` 包括初始加载，后续版本单独统计播放中再缓冲。
远程画面测试观察到播放完成；未进行本地拖点 input-to-paint 的 p95 测量。

不能由这些数字断言 WAN 比普通网络更快，也不能将不同方案的峰值/均值相除作为带宽节省比。
短片、浏览器显示状态、预览尺寸和 UI 绘制方式均会影响结果。

## 业务链路

- 两个浏览器会话同时运行原版 Label 和 QC，分别使用 DISPLAY 92/91、独立缓存/身份。
- Label 加载测试任务，修改可见性、保存 3 帧、完成提交。
  测试目录生成 15 个坐标数组（5 相机 × 3 帧），形状 `(2,21,2)`，以及独立可见性文件。
  `manual_label=succeeded`，仅在测试库创建一个 `manual_3d=queued`。
- QC 后端完成真实六视图渲染，浏览器播放至第 179 帧后提交，显示“后端已确认 QC 结果”。
- 测试输出写入 `fixture-v2/nas/pilot/...`，不修改源采集 Episode 或生产工作流状态。
- 自动检查：16 项 remote/frontend lifecycle 测试通过；实际 HTTP Range 请求返回 206 和正确 100 字节范围。
- 仍未验收：真实多人权限边界、长视频并发、丢包、外包网络、原生浏览器标注/质检完整功能、
  CoTracker 全流程，以及输入响应/冷跳转的 p95。

## 本机证据

`/Users/cactusxiao/Desktop/demo/output/outsourcing-validation/` 保存本机浏览器截图和测试结果。
远程工作目录保留独立的业务结果、派生预览和会话日志，便于复查；会话连接令牌不写入本报告。
验证结束后已关闭两个 GUI 会话及测试工作流服务，确认两份临时缓存和连接凭据已删除。
原生产后端与原 QC 进程仍在运行。仅保留低负载的派生视频实验服务和本机隧道供查看。

## 参考

- [noVNC 官方架构与嵌入说明](https://novnc.com/noVNC/docs/EMBEDDING.html)
- [视频帧呈现回调](https://developer.mozilla.org/en-US/docs/Web/API/HTMLVideoElement/requestVideoFrameCallback)
- [浏览器视频播放质量指标](https://developer.mozilla.org/en-US/docs/Web/API/VideoPlaybackQuality)
- [MSE 点播与实时传输的区别](https://developer.mozilla.org/en-US/docs/Web/API/Media_Source_Extensions_API)

## 本地草稿 / 批量提交的实机追加验证

2026-09-09 使用新增的原生浏览器分支，在 `fixture-batch` 独立数据库和 NAS 测试目录完成：

| 场景 | 实际结果 |
|---|---|
| 后端关闭后编辑 Label | 仍能拖动关节、修改可见性、确认本帧 |
| 后端关闭时刷新页面 | 应用页面、已缓存五相机画面、草稿均恢复；已确认 1/3 帧 |
| 恢复后的坐标 | 00 相机第 0 帧左手 thumb_mcp 显示 259.3, 294.0；可见性为 false |
| 提交前检查后端 | manual_label 仍为 running；正式坐标文件 0；提交回执 0 |
| 恢复连接并提交 Label | 15 份 float32 坐标和 15 份 uint8 可见性文件；恰好一条 manual_3d queued |
| 文件内容核对 | 对应关节保存为 [259.3162536621094, 294.01708984375]；可见性 0 |
| QC 六视角派生视频 | 实际播放 60 帧；进度达到 60/60；Ego 10–12 区间先留在本机 |
| QC 提交时关闭后端 | 显示重试提交；刷新后保留 60/60、Ego 10–12 和同一提交编号 |
| 恢复连接重试 QC | 收到回执；QC passed=true；Ego 报告独立记录 10–12；库中共两张回执 |
| 第二标签页打开同一草稿 | 被编辑锁阻止，原标签页可继续操作 |

本轮自动检查共 91 项，86 项通过，5 项因运行环境跳过。
新增覆盖任务过期和被桌面接走、源文件版本变化、错误帧/相机/坐标、重复并发提交、
HTTP 响应丢失后重试、鉴权/Origin、Range，以及提交异常时数据库和结果文件回滚。

前述两份已完成测试任务保留为审计样本；另建立 `try-label` 和 `try-qc` 隔离试用任务。
本机截图位于 `output/outsourcing-validation/batch-offline-restored.png`、
`batch-label-receipt.png`、`batch-qc-receipt.png`。
浏览器试用服务使用 18881，旧媒体性能实验继续使用 18880，两个入口不同。
本轮没有修改原 Label/QC 代码或配置；正式后端 PID 4105787 与原 QC PID 4106171 在联机检查时仍在运行。

收尾时本机到实验室主机 10.162.241.5 的网络中断，SSH 重连超时，18881 的本机隧道已断开。
上述联机验证在中断前完成；当前浏览器展示 `try-label` 的离线缓存草稿。
最后两处网页异步保护（离开任务时忽略迟到的 samples 响应、忽略无活动任务的关节刷新）
及本次文档更新仅保存于本机，尚待网络恢复后同步源码快照。没有修改正式服务来处理此连接问题。


## 2026-09-10 VPN 恢复后的同步

VPN 恢复后，SSH 已重新连通；18880 媒体实验和 18881 外包工作台隧道均已恢复，增加 SSH 心跳检测。
前一日未同步的网页异步保护已同步到隔离源码快照；网页脚本、页面缓存脚本及两份后端核心文件的 SHA-256 与本机一致。
浏览器刷新后成功恢复 `try-label` 草稿并续租，能够继续请求第 1 帧。
原正式后端 PID 4105787 和 QC PID 4106171 仍在运行，本轮未重启或修改它们。

同步后复查：`try-qc` 的六视角预览播放至 60/60，末帧编号和六张静态图均为 59；逐帧后退定位至帧 57。此次仅检查隔离试用草稿，没有提交新的正式结果。

旧 18880 视频实验服务在断线后返回空响应；本次仅重启该独立测试进程，并将日志重定向到测试目录，已恢复 HTTP 200。18881 工作台和原正式后端/QC 未重启。


## 2026-09-10 接入真实任务后端

用户要求测试真实后端后，外包模块已单独部署至正式仓库，监听 18882。部署前使用实际正式仓库依赖
运行兼容测试，修复旧版可见性目录与 artifact 类型差异后，11 项测试全部通过。
正式库启动前已通过 SQLite backup 保存一致性备份；仅新增浏览器会话/回执表，不执行原后端启动迁移。
已用真实 8765 /api/v1/jobs 接口与目标库的任务 ID / Episode 对照确认连接一致。
首次打开显示 3 个可领取的真实标注任务，界面明确标识“真实后端”。检查时浏览器会话和回执均为 0，
没有替用户领取、编辑或提交真实业务任务。原后端 PID 4105787 与 QC PID 4106171 继续运行。
Git 状态仍是新增 remote_frontend 目录；此次部署不等同于 Git 拉取合并。

本机追加回归首轮共 92 项：86 项通过、5 项跳过、1 项原有自动领取排序测试失败；该用例单独重试通过。
仅导入原有后端和原有测试、将两条任务创建时间固定为先后两秒后，可稳定复现其按创建时间而非 Episode 序号领取的差异。
外包入口按用户选中的 job_id 精确领取，新增目标仓库兼容测试 11 项全部通过；此次未修改原有排序代码或测试。

## 2026-09-10 可操作外包工作台更新

本轮将网页操作扩展为任务筛选、标注帧进度、多视角总览/单机位编辑、缩放平移、关节可见性与可见机位计数、隐藏手部/忽略视角、来源对照、后端三维骨架/MANO/CoTracker，以及 QC 单帧/十帧步进、速度控制、区间起止点、单机位放大和三秒分段播放。

独立环境 `fixture-ui-v2` 使用真实视频的只读引用、复制元数据和独立工作流库。本机通过 VPN/SSH 访问实验室的 18883 验证实例：

- 原始 MANO 渲染成功，返回图像到网页；计算没有写入标注结果目录。
- 在 00 相机将左手 wrist 选为跟踪点，后端 CoTracker 成功从帧 0 跟踪到帧 1，浏览器显示坐标 251.9, 307.2，结果先保存在本机。
- 提交前后端坐标文件和 QC 报告均为 0；显式提交后生成 15 份坐标、15 份可见性文件，并恰好生成一个 manual_3d 任务。对应坐标实际为 [251.89329528808594, 307.1837158203125]。
- QC 180 帧生成两个 90 帧片段，单段编码耗时 2.160 秒、1.806 秒，分别 41.67、49.85 fps；平均编码码率约 3.07 Mbps。这些是派生编码数据，不包含所有原始解码/渲染耗时。
- 浏览器通过 MSE 播放两个片段，完整播放计数为 180/180；质量计数显示 0 丢帧。帧 89 到帧 90 的定位时间分别 2.975 秒、3.008333 秒，对应静态图编号一致。
- 修复起播前已显示首帧未计入播放进度的问题；修复快速步进时区间控件可能读取旧值的问题，设置边界需等精确帧就绪，并从已保存的区间状态读取提交值。
- 快速连续操作正确产生 MANO 90–100；此前创建的 Ego 90–90 独立保留。刷新后草稿和区间恢复。显式提交后两份 JSON 报告分别记录这些范围，库中共两张回执。
- Ego 机位静态图可打开独立放大检查窗口。验证页已经关闭，结果与回执保留供核查。

上述是当前 VPN、实验室主机和本机上的短视频验证，不代表弱网、低配机或多操作员并发 SLA。页面的片段读取速度可能来自浏览器缓存，不作为实网带宽证据。

本轮回归共 75 项，70 项通过、5 项环境跳过；随后新增旧数据参考缺失回归。最新 `test_browser_batch` 共 14 项，在本机和正式仓库依赖下均通过，包含计算请求范围/坐标校验、计算不发布业务文件和旧任务参考缺失时保留已有修改。

正式入口仍为 18882，代码部署至 `Orbbec_demo/remote_frontend`；只重启外包服务。对比 49 份原 Label/QC/后端及关联文件的哈希，均未改变；原后端 PID 4105787 和 QC PID 4106171 继续运行。此次仍为独立目录部署，没有 Git 提交、推送或拉取合并。

为用户打开真实 `task2 / Episode 19` 后发现它缺少 `joints_vis`，但已有六份历史标注文件。外包分支已对齐旧 Label：保留已有修改；参考缺失时显示明确提示，缺失初始值使用不可见空关节。六个机位、21 帧的浏览器数据已准备完成，停留在第 0 帧的 00 机位编辑界面，进度 0/21。此次没有修改其标注或提交业务结果；六份历史文件哈希保持一致。正式库现有一个浏览器会话、零张浏览器提交回执。

当前工作台截图：`output/outsourcing-validation/real-label-workbench.png`。
正式部署清单、原文件哈希、日志和连接文件位于 `/home/ubuntu/demo/outsourcing-live-20260910/`。

## 2026-09-10 v3：以原 Label/QC 操作为规范的修正

用户明确要求原版 UI、步骤和行为保持一致，因此替换 v2 自行设计的网页流程。
以本地 `label/app.py`、`label/canvas_view.py`、`src/qc/app.py` 为对照，恢复原页面层级、工具栏、
手型交互、确认快照、跟踪和 QC 两种操作模式；功能与边界见 BROWSER.md。

最终自动检查：新增分支 34 项全部通过；原 Label/QC 和生命周期 57 项中 52 项通过、5 项环境跳过。
实验室正式仓库依赖下 18 项浏览器批量/HTTP/权限兼容检查全部通过，使用临时数据库。
QC 状态与合并区间测试直接调用原桌面方法；新增 WebKit 双击回归覆盖 PointerEvent.detail=0 的实际问题。

独立验证根目录为 `/home/ubuntu/demo/outsourcing-validation-20260909/browser-ui-v3`。
使用真实视频的只读引用、复制元数据和独立工作流库，全部测试提交只写入该副本：

- Label 默认单机位；右键手型进入定位、右键图像放置、撤销、数字 0 总览通过实测。
- 在原始预览下确认仍保存修改后快照；3 帧全部确认时服务器无输出，最终提交后生成 15 份坐标、15 份可见性及一张回执。
- 原手型双击不会再变成两次可见性切换。选择 00 相机左手 wrist 后确认帧 11，后端 CoTracker 成功更新帧 12，网页自动前进，结果保留本机且未提交。
- Show MANO 在服务器渲染并返回；修改后来源有两个关节不足两路可见时 Skeleton 按原规则拒绝，切换原始来源后成功重建五机位骨架。显示 Skeleton 时确认按钮禁用。
- QC 六机位准备成功，暂停进入坏帧模式时 Start/End 为空；从帧 10 回看最多到帧 1，确认 Ego 1–11，手部 11–21；撤销返回进入位置。
- 实际播放到帧 179 后可提交；回看帧 178、返回释放、重新领取恢复后，范围、当前帧和已播放完成状态保留。
- 最终 QC 提交分别写出手部 11–21 和 Ego 1–11，两张业务回执分别对应 Label/QC。未把 Ego 区间写进手部报告。
- 跨页面测试发现并修复 QC 提交后 Label 时间轴继承禁用状态。修复最后一个 Episode 完成后跳回 Task 页的问题，保留 Episode 列表层级。
- 新浏览器来源直接访问普通 URL，首先显示登录页；独立测试账号登录后正常显示任务。账号改密、权限、停用、过期及越权由真实 HTTP 和独立账号库测试覆盖。

验证期间实验室主机重启，旧浏览器、后端和 QC 进程退出；随后发现原后端已由其他流程恢复为 PID 20349。
本次没有启动或修改原 Label/QC；只恢复并更新外包服务。v3 真实入口监听 127.0.0.1:18882，部署时 PID 22156。
前后比较的 49 份原文件哈希一致；task2 / Episode 19 六份历史标注哈希一致。
部署前后正式库均为 109 个 jobs、2 个 browser_sessions、0 个 browser_receipts，没有新增正式提交。
部署是独立目录替换，不是 Git 提交、推送或合并。旧外包代码和一致性数据库备份位于 live 目录。

账号库与工作流库分离。首次 admin 保留旧 `browser-local-test` 身份映射，便于接续旧草稿；
已验证正式入口的初始登录和强制改密页，新密码由用户本人设置。未领取或提交新的真实任务。
企业 VPN 的公共 HTTPS 域名/证书尚未部署；当前普通本机入口经 SSH 加密连接实验室，不能把 127.0.0.1 地址直接发给供应商。

证据位于本机 `output/outsourcing-validation/` 的 native-label-*、native-qc-workflow-v3.png，
以及远端 live 目录的 deployment-native-ui-v3.json、validation-native-ui-v3.json。
这里验证的是实际短片、选定交互与业务结果，不宣称已完成所有尺寸的逐像素对照、真实 Safari 长片及多人弱网压力验收。


## 2026-09-10 原后端账号接入修正

定位：`xjz` 在原 8765 `/api/v1/auth/login` 返回 200，而外包账号库此前只有 admin。
因此失败原因是遗漏原账号验证接入，不是用户密码错误。

新增显式账号关联，使用现有 `task_backend_state_nas6/accounts.json` 与原 AccountStore 密码算法，
不复制凭据、不改原账号文件。`xjz` 关联为 Label/QC 测试用户，无管理员权限。
原账号改密、删除、来源不可用、外包停用/缩权、来源冲突、禁止在外包端重置原密码均有回归覆盖。
本机 30 项账号/批量回归通过；增加 HTTP 关联登录检查后该用例通过；最终实验室旧仓库依赖下 30 项全部通过。
只替换外包认证服务与账号页面四份文件，备份位于 `before-backend-account-link/`。
部署比较的 49 份原文件、原 accounts.json 均未变，没有重启原后端或原 Label/QC。
