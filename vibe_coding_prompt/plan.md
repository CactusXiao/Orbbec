**在已有代码的功能基础上进行修改，不得破坏已有代码的功能，不需要在本地编译运行代码**
在原始collection.cpp的基础上，修改RGB预览与数据采集的逻辑，要求不得破坏原代码已有功能。
1. 修改RGB预览逻辑，将其与交互式可视化中的RGB预览实现对齐，不在回调线程中做解码，而是像交互式可视化一样在UI线程中进行解码
2. 修改录制逻辑，在worker线程中进行解码，回调线程只缓存指针。由于录制时需要将获取的图片暂存在内存中，所以为防止SDK栈溢出，需要在将某一帧从SDKbuffer拷贝到暂存区后，立即释放对SDKbuffer该帧对引用。