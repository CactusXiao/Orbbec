**代码会在远程主机上编译运行，不要试图在本地编译或运行代码**

请按照文档要求，针对本项目的collection功能模块完成如下代码修改：

1. 首先阅读collection.cpp文件，理解采集逻辑以及数据从内参到磁盘的落盘逻辑。
2. 完成如下采集过程中的操作逻辑重构：
- 在collection模块的UI中，新增键盘读取逻辑，将键盘的ctrl + 1-4分别映射成collection UI中的start,stop,save,reset按钮，当按下ctrl+ 1-4快捷键后，能够起到等效于点击对应按钮的效果
- 在collection的配置界面曝光时间选项的初始值由0.8设置为0.1
- 在采集的UI界面，不再要求在左上角输入框中输入task id，而是从save_path下读取一个名为task.json的json文件，自动从中获取任务名以及任务描述。json文件内容示例如/Users/wc/Desktop/orbbec/tasks.json中所示，有的任务可能需要重复多次，重复次数由文件中的repeat_times进行配置。为保证采集中断后能够从上次任务处开始，需要在save_path/subject_id目录下维护一个csv文件，每完成一次采集，根据任务名称以及任务重复次数，在文件中更新如下内容：“task_name  n/task_repeat_times”，其中n为任务重复次数。当一个任务重复次数达到要求后，根据tasks.json文件中的内容，取出一个新的task。每次从cofig进入采集UI界面，都从csv文件中读取当前采集的指针，从上一次中断的任务进度继续采集。
- UI界面的布局需要进行重构。多相机RGB实时可视化窗口固定左上角不动，尺寸缩小到原来的1/2，画面尺寸大小需要根据可视化窗口大小进行调整。调整后，UI的右侧和可视化窗口下侧会空出一部分区域，我希望在右侧区域中能够用醒目的字体显示当前任务及其的中文任务描述，对应json文件中的任务名以及task_discription_cn。可视化窗口的下侧用绿/黄/红/白四种颜色醒目地用大字体显示当前状态，理想的操作逻辑/Users/wc/Desktop/orbbec/logic.md，按照操作逻辑轮流点击四种按钮会进入四种状态，无论何种状态下点击reset都会进入到保存完毕待采集状态，请你自行设计状态图以及转移逻辑，将非法操作无效化。
3. 完成如下保存过程中的逻辑重构：
- 保存路径不再是save_path/subject_id/task_id下面存RGB Depth，而是新增一级目录用来记录同一个任务的序号,即保存路径变成了save_path/subject_id/task_id/eposide_n，其中n是当前采集的任务重复数。
- 点击保存按钮后，在保存深度图时对深度图进行后处理，不再保存完整的深度图，而是根据深度相机、RGB相机的内外参，以及两种相机的坐标系变换，计算出深度图中的像素在RGB像素坐标系中的对应坐标，将深度图对齐到RGB像素空间，并将深度图移出RGB像素空间外的部分裁剪掉，在深度图缺失的部分进行补全，以保证裁剪后的深度图依旧于RGB有相同分辨率，此外相机参数的保存逻辑也相应改变，由原来的保存RGB和深度相机的内参+RGB与深度相机的坐标系变换，变为只保存RGB相机的内参。代码中相机参数内容参考/Users/wc/Desktop/orbbec/camera_params.json，其中"d2c_extrinsic"表示从深度相机的相机坐标系到RGB相机的相机坐标系的变换，平移的单位是mm，使用时需要做转换。
4. 审查代码，找出当前代码存在的效率问题，解决程序在点击保存按钮后需要等待很长时间才能开始下一段采集的问题。
