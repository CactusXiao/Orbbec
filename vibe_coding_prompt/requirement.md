请你在当前交互式可视化的基础上，实现以下功能:
1. 在sync目录下，编写一个python程序，其功能是实时读取交互式可视化代码采集到的RGBD，利用mediapipe进行实时推理，计算出所有第三人称视角下可见手关节点的2d坐标。其中，第三人称视角指的是在extrinsic文件中有外参信息的相机。结合第三人称相机外参，利用triangulation计算出所有标注出的2d关节点的3d坐标。
2. 在interactive_visualization.cpp中，编写对应的接口，并在交互式可视化界面中，添加visualizing GT的功能，当勾选该功能时，交互式可视化中的RGB流被组织成python程序期望的形式，送到python程序中，然后python程序计算出场景中所有关节点的3d坐标，并将其返回给交互式可视化程序，在可视化程序的点云中，用不同颜色的小球标识出关节所在的位置。
要求：
1. 能够正确实现双手关节点的显示，正确区分左右手
2. 显示频率能够到达30hz，最低不低于20hz，python代码处理效率，进程通信效率足够高。
3. 可视化时的内存与cpu资源充裕，可以用资源换性能
4. 代码会在远程服务器跑，不要在本地编译运行
5. 只允许修改交互式可视化功能，不允许修改其他功能